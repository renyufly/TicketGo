TicketGo/
├── .cache/                                         —— 存放项目内 Go/npm 模块、编译、测试和分析缓存，不属于业务源码。
│   ├── go-build/                                   —— Go 常规构建与测试使用的项目级缓存目录（内部生成文件不逐项列出）。
│   ├── go-build-build/                             —— Go build 命令使用的独立项目级构建缓存目录（内部生成文件不逐项列出）。
│   ├── go-build-vet/                               —— Go vet 命令使用的独立项目级分析缓存目录（内部生成文件不逐项列出）。
│   ├── go-mod/                                     —— Go 依赖模块的项目级下载缓存目录（内部第三方文件不逐项列出）。
│   └── npm/                                        —— npm 的项目级下载与元数据缓存目录（内部第三方文件不逐项列出）。
├── .git/                                           —— 保存 Git 版本库对象、索引和引用等内部元数据（内部生成文件不逐项列出）。
├── .github/                                        —— 保存 GitHub 平台相关配置。
│   └── workflows/                                  —— 保存 GitHub Actions 工作流。
│       └── ci.yml                                  —— 定义 Go 与 React 的格式、lint、测试和生产构建 CI 门禁。
├── .tools/                                         —— 存放不会影响系统全局环境的项目内工具链。
│   ├── go/                                         —— 存放项目锁定的便携版 Go 1.27.0 工具链（官方内部文件不逐项列出）。
│   └── k6/                                         —— 存放项目锁定并校验的便携版 k6 v2.2.0（官方二进制不展开）。
├── bin/                                            —— 存放本地构建产物。
│   ├── phase2lab.exe                               —— Windows 下构建的 Phase 2 数据准备、校验与 PostgreSQL 实验工具。
│   └── ticketgo.exe                                —— Windows 下构建得到的 TicketGo 服务可执行文件。
├── cmd/                                            —— 存放可执行程序入口。
│   ├── phase2lab/                                  —— Phase 2 可复现数据与数据库实验 CLI。
│   │   └── main.go                                 —— 实现 prepare、verify、monitor、internals 等子命令。
│   └── server/                                     —— TicketGo HTTP 服务入口目录。
│       └── main.go                                 —— 初始化日志和配置、监听退出信号并启动应用。
├── docs/                                           —— 存放架构、接口、数据库和阶段实验文档。
│   ├── adr/                                        —— 存放架构决策记录。
│   │   └── 0001-inventory-concurrency-control.md   —— 记录条件原子 UPDATE 的选型数据、代价与回滚方案。
│   ├── database/                                   —— 存放 PostgreSQL 内部机制与数据库对比文档。
│   │   ├── mysql-vs-postgresql.md                  —— 对比 PostgreSQL 与 MySQL/InnoDB 的 MVCC、锁和索引差异。
│   │   └── postgresql-internals.md                 —— 解释 MVCC、VACUUM、WAL、EXPLAIN 和索引类型实测。
│   ├── postmortems/                                —— 存放故障实验的复盘记录。
│   │   ├── phase1-postgresql-outage.md             —— 记录 Phase 1 PostgreSQL 停机、检测和恢复实验。
│   │   └── phase1-transaction-rollback.md          —— 记录 Phase 1 事务中途失败及完整回滚实验。
│   ├── results/phase2/                             —— 保存 Phase 2 可提交的汇总与 PostgreSQL 原始计划。
│   │   ├── concurrency-summary.json                —— 汇总四种策略三轮指标与正确性结果。
│   │   ├── index-types-explain.txt                 —— 保存六类 PostgreSQL 索引的 EXPLAIN 输出。
│   │   ├── order-index-explain.txt                 —— 保存百万订单联合索引前后和写代价输出。
│   │   └── postgresql-internals.json               —— 保存 MVCC、Dead Tuple/VACUUM 和 WAL 结构化结果。
│   ├── api.md                                      —— 说明 Phase 2 API、并发错误、权限和分页规则。
│   ├── database.md                                 —— 说明业务表字段、约束、索引、状态机和取消规则。
│   ├── phase0-bootstrap.md                         —— 说明 Phase 0 工程骨架、启动链路和健康检查设计。
│   ├── phase1-monolith.md                          —— 总结 Phase 1 单体 MVP 的实现、测试、取舍和已知并发缺陷。
│   ├── phase1b-frontend.md                         —— 说明 React 演示页面的环境、结构、安全边界、启动和验证结果。
│   └── phase2-concurrency-report.md                —— 汇总固定负载、三方案对比、数据库瓶颈与阶段结论。
├── internal/                                       —— 存放仅供本项目内部使用的应用业务代码。
│   ├── activity/                                   —— 活动领域模块。
│   │   ├── handler.go                              —— 处理活动创建、列表和详情 HTTP 请求。
│   │   ├── model.go                                —— 定义活动实体及创建活动输入 DTO。
│   │   ├── repository.go                           —— 使用 PostgreSQL 实现活动与库存的数据访问。
│   │   ├── service.go                              —— 实现活动参数校验、默认状态和领域错误转换。
│   │   └── service_test.go                         —— 测试活动时间范围等 service 业务校验。
│   ├── app/                                        —— 应用装配和生命周期模块。
│   │   └── app.go                                  —— 建立数据库连接、装配路由并管理 HTTP 服务优雅退出。
│   ├── auth/                                       —— 身份认证与授权模块。
│   │   ├── middleware.go                           —— 校验 Bearer Token、用户状态和管理员权限。
│   │   ├── token.go                                —— 使用 HMAC-SHA256 签发和解析带过期时间的 JWT。
│   │   └── token_test.go                           —— 测试 JWT 签发、解析和过期拒绝行为。
│   ├── config/                                     —— 环境变量配置模块。
│   │   ├── config.go                               —— 加载并校验 HTTP、数据库和认证配置。
│   │   └── config_test.go                          —— 测试默认配置及非法配置的快速失败行为。
│   ├── domain/                                     —— 跨业务模块共享的领域错误定义。
│   │   └── errors.go                               —— 定义无效参数、冲突、售罄和活动不可用等稳定错误。
│   ├── health/                                     —— 服务健康检查模块。
│   │   ├── handler.go                              —— 实现 liveness 与 PostgreSQL readiness 接口。
│   │   └── handler_test.go                         —— 测试依赖故障时 readiness 返回 503 且不泄露内部错误。
│   ├── httpapi/                                    —— HTTP 路由及通用 Web 能力模块。
│   │   ├── apierror/                               —— HTTP 错误映射子模块。
│   │   │   └── apierror.go                         —— 把领域错误和校验错误转换成稳定的 HTTP JSON 响应。
│   │   ├── middleware/                             —— Gin 通用中间件目录。
│   │   │   ├── access_log.go                       —— 记录包含 request_id、状态码和耗时的结构化访问日志。
│   │   │   ├── recovery.go                         —— 捕获 panic、记录堆栈并返回统一 500 响应。
│   │   │   ├── request_id.go                       —— 接收或生成请求 ID 并写入上下文和响应头。
│   │   │   ├── request_id_test.go                  —— 测试调用方传入的请求 ID 会被正确保留。
│   │   │   └── timeout.go                          —— 为每个 HTTP 请求注入有截止时间的 context。
│   │   ├── pagination/                             —— 列表分页参数解析子模块。
│   │   │   └── pagination.go                       —— 校验 limit 和 offset 的取值及硬上限。
│   │   ├── router.go                               —— 装配健康、用户、商品、活动和订单路由及权限中间件。
│   │   └── router_test.go                          —— 测试 live/ready 语义分离及请求 ID 行为。
│   ├── inventory/                                  —— 库存领域模块。
│   │   ├── model.go                                —— 定义秒杀所需的活动与库存联合状态。
│   │   └── repository.go                           —— 实现普通/锁定读取、朴素、条件 UPDATE、CAS 和回补 SQL。
│   ├── item/                                       —— 商品领域模块。
│   │   ├── handler.go                              —— 处理商品创建、列表和详情 HTTP 请求。
│   │   ├── model.go                                —— 定义商品实体及创建商品输入 DTO。
│   │   ├── repository.go                           —— 实现商品的 PostgreSQL 创建和查询操作。
│   │   ├── service.go                              —— 实现商品名称、价格和默认状态业务校验。
│   │   └── service_test.go                         —— 测试商品非法价格会被 service 拒绝。
│   ├── order/                                      —— 秒杀与订单领域模块。
│   │   ├── concurrency.go                          —— 定义四种库存策略及超时、重试、退避配置。
│   │   ├── handler.go                              —— 处理秒杀、订单列表、详情和取消 HTTP 请求。
│   │   ├── integration_test.go                     —— 真实 PostgreSQL 测试回滚、超卖复现、三方案不变量和锁超时。
│   │   ├── model.go                                —— 定义订单实体及秒杀输入 DTO。
│   │   ├── repository.go                           —— 实现订单和秒杀记录的 PostgreSQL 数据访问。
│   │   ├── service.go                              —— 编排库存并发策略、事务、订单与取消，并映射受控竞争错误。
│   │   └── service_test.go                         —— 单测活动状态、库存、数量和重复用户等秒杀规则。
│   └── user/                                       —— 用户领域模块。
│       ├── handler.go                              —— 处理注册、登录和当前用户 HTTP 请求。
│       ├── model.go                                —— 定义用户实体、注册 DTO 和登录 DTO。
│       ├── repository.go                           —— 实现用户创建及按邮箱或 ID 查询的 PostgreSQL 操作。
│       ├── service.go                              —— 实现邮箱规范化、bcrypt 密码哈希、登录和 Token 签发。
│       └── service_test.go                         —— 测试密码哈希、邮箱规范化和无效登录行为。
├── migrations/                                     —— 存放 golang-migrate 管理的数据库版本脚本。
│   ├── 000001_bootstrap.down.sql                   —— 回滚 Phase 0 空操作 bootstrap migration。
│   ├── 000001_bootstrap.up.sql                     —— 建立 Phase 0 migration 管理流程而不创建业务表。
│   ├── 000002_phase1_monolith.down.sql             —— 删除 Phase 1 全部业务表以回滚 schema。
│   └── 000002_phase1_monolith.up.sql               —— 创建 Phase 1 六张业务表、约束和索引。
├── pkg/                                            —— 存放可被项目多个内部模块复用的基础包。
│   ├── database/                                   —— PostgreSQL 连接池基础包。
│   │   └── database.go                             —— 创建、配置和探测 database/sql + pgx 连接池。
│   ├── logger/                                     —— 结构化日志基础包。
│   │   └── logger.go                               —— 创建统一配置的生产级 zap logger。
│   └── response/                                   —— HTTP JSON 响应基础包。
│       └── response.go                             —— 输出统一成功/错误结构并携带 request_id。
├── scripts/                                        —— 存放项目环境辅助脚本。
│   ├── sql/                                        —— Phase 2 隔离 PostgreSQL 实验 SQL。
│   │   ├── phase2-index-types.sql                  —— 建立 B-Tree/GIN/GiST/BRIN/Partial/Expression 实验与 EXPLAIN。
│   │   └── phase2-order-index.sql                  —— 生成百万订单并比较联合索引读写与空间代价。
│   ├── bootstrap-go.ps1                            —— 下载、校验并解压项目内锁定的 Windows Go 工具链。
│   ├── bootstrap-k6.ps1                            —— 下载、校验并解压项目内锁定的 Windows k6 工具链。
│   ├── reproduce-phase2.ps1                        —— 一条命令复现 Phase 2 正式负载、低冲突对照、汇总和 PostgreSQL 实验。
│   ├── run-phase2-load.ps1                         —— 自动执行数据重建、四策略多轮 k6、监控与正确性校验。
│   ├── run-phase2-postgresql-labs.ps1              —— 自动执行 PostgreSQL 内部机制、百万订单和索引实验。
│   └── summarize-phase2-results.ps1                —— 从忽略的 raw 文件生成可提交的结构化汇总。
├── tests/                                          —— 存放跨模块测试。
│   ├── e2e/                                        —— 端到端测试目录。
│   │   └── phase1_test.go                          —— 使用真实 PostgreSQL 验证 Phase 1 闭环及 Phase 1B admin 注册开关。
│   └── load/                                       —— 保存可重复负载测试脚本。
│       └── phase2-seckill.js                       —— 以唯一测试用户执行固定 1000 抢 100 并输出 k6 指标。
├── web/                                            —— Phase 1B React + Vite + TypeScript 演示前端。
│   ├── e2e/                                        —— 存放连接真实 Gin/PostgreSQL 的最小演示脚本。
│   │   └── demo-flow.mjs                           —— 验证注册、商品、活动、秒杀、查单、取消和库存回补闭环。
│   ├── src/                                        —— 前端 TypeScript/React 源码。
│   │   ├── api/                                    —— API 契约、统一 Client、格式化和 API 单测。
│   │   │   ├── client.test.ts                      —— 测试标准成功 envelope 和稳定错误/request_id。
│   │   │   ├── client.ts                           —— 附加 JWT、解包响应并统一转换 API 错误。
│   │   │   ├── format.ts                           —— 格式化 cents 金额、RFC3339 时间和本地时区。
│   │   │   └── types.ts                            —— 定义 User、Item、Activity、Order 和健康状态类型。
│   │   ├── auth/                                   —— 登录 Token、当前用户和身份恢复状态。
│   │   │   └── AuthContext.tsx                     —— 管理 session JWT、最近邮箱、登录、恢复和退出。
│   │   ├── components/                             —— 页面共享的轻量 UI 组件。
│   │   │   ├── Entity.tsx                          —— 页面标题、状态标签和详情字段组件。
│   │   │   ├── Feedback.test.tsx                   —— 测试用户可读错误和 request_id 展示。
│   │   │   ├── Feedback.tsx                        —— loading、empty、success 与错误追踪反馈。
│   │   │   ├── Layout.tsx                          —— 登录后的导航、账号切换和页面框架。
│   │   │   └── Pagination.tsx                      —— 提供固定 limit/offset 上下页交互。
│   │   ├── features/                               —— 按业务领域组织的 API 调用。
│   │   │   ├── activities/api.ts                   —— 活动列表/详情/创建和秒杀请求。
│   │   │   ├── items/api.ts                        —— 商品列表/详情/创建请求。
│   │   │   ├── orders/api.ts                       —— 订单列表/详情/取消请求。
│   │   │   └── users/api.ts                        —— 注册、登录和当前用户请求。
│   │   ├── pages/                                  —— 覆盖全部 Phase 1 API 的路由页面。
│   │   │   ├── ActivitiesPage.tsx                  —— 活动分页列表与库存摘要页面。
│   │   │   ├── ActivityDetailPage.tsx              —— 活动详情、库存和同步秒杀页面。
│   │   │   ├── CreateActivityPage.tsx              —— admin 创建活动与库存表单。
│   │   │   ├── CreateItemPage.tsx                  —— admin 创建商品表单。
│   │   │   ├── ForbiddenPage.tsx                   —— 展示前端权限不足提示。
│   │   │   ├── ItemDetailPage.tsx                  —— 商品详情页面。
│   │   │   ├── ItemsPage.tsx                       —— 商品分页列表页面。
│   │   │   ├── LoginPage.tsx                       —— 登录、最近邮箱回填和角色跳转页面。
│   │   │   ├── NotFoundPage.tsx                    —— 未匹配路由页面。
│   │   │   ├── OrderDetailPage.tsx                 —— 订单详情、取消和库存刷新页面。
│   │   │   ├── OrdersPage.tsx                      —— 当前用户订单分页列表页面。
│   │   │   ├── ProfilePage.tsx                     —— 当前用户 ID、邮箱、角色和状态页面。
│   │   │   ├── RegisterPage.tsx                    —— customer/admin 受控注册页面。
│   │   │   └── SystemStatusPage.tsx                —— 展示 live、ready 与 PostgreSQL 状态。
│   │   ├── router/                                 —— React Router 与访问控制。
│   │   │   ├── AppRouter.tsx                       —— 装配公开、登录、admin 与详情路由。
│   │   │   └── ProtectedRoute.tsx                  —— 执行身份恢复和 admin 页面保护。
│   │   ├── styles/global.css                       —— 全局设计变量、布局、状态和响应式样式。
│   │   ├── test/setup.ts                           —— 初始化 Vitest DOM matcher 与测试清理。
│   │   └── main.tsx                                —— 装配 Query、Router、Auth 与 React 根节点。
│   ├── .dockerignore                               —— 排除前端依赖、构建产物和环境文件。
│   ├── .env.example                                —— 提供前端 admin 选项显示开关示例。
│   ├── .node-version                               —— 锁定项目 Node.js 24.11.1。
│   ├── .npmrc                                      —— 固定项目 npm 缓存并关闭非必要联网提示。
│   ├── .prettierignore                             —— 排除生成产物与 lockfile 的格式化。
│   ├── .prettierrc.json                            —— 让 Prettier 在 Windows/Linux 自动沿用文件换行，保证本地门禁一致。
│   ├── Dockerfile                                  —— 用锁定 Node 构建并由 Nginx 提供静态页面。
│   ├── eslint.config.js                            —— 配置 TypeScript、React Hooks 与 Vite lint。
│   ├── go.mod                                      —— 隔离根 Go module 与 node_modules 第三方 Go 示例。
│   ├── index.html                                  —— Vite SPA HTML 入口。
│   ├── nginx.conf                                  —— 配置 SPA fallback 与 Gin API 反向代理。
│   ├── package-lock.json                           —— 精确锁定完整 npm 依赖树。
│   ├── package.json                                —— 声明 Node/npm、依赖与前端命令。
│   ├── tsconfig.json                               —— 启用严格 TypeScript 和 Bundler 解析。
│   └── vite.config.ts                              —— 配置 React、开发代理和 Vitest jsdom。
├── .dockerignore                                   —— 指定 Docker 构建上下文中需要排除的本地文件。
├── .env                                            —— 保存当前机器的本地开发环境变量和密钥，不提交版本库。
├── .env.example                                    —— 提供可复制的环境变量模板和安全配置说明。
├── .gitignore                                      —— 排除本地环境、缓存、工具链和构建产物。
├── docker-compose.yml                              —— 定义本地 PostgreSQL 17.6 服务、健康检查和持久卷。
├── Dockerfile                                      —— 使用多阶段构建生成 TicketGo Linux 容器镜像。
├── go.mod                                          —— 声明 Go 版本、module 名称和直接/间接依赖。
├── go.sum                                          —— 锁定并校验 Go 依赖模块内容。
├── Idea.md                                         —— 记录项目背景、学习路线和高并发抢票系统总体构想。
├── LICENSE                                         —— 声明项目的 MIT 开源许可证。
├── Makefile                                        —— 提供 Go/React、Phase 2 负载/数据库实验、构建、Compose 和 migration 命令。
├── plan.md                                         —— 定义 TicketGo 从 Phase 0 到后续分布式阶段的严格执行计划。
├── README.md                                       —— 提供 Phase 2 环境、启动、并发/数据库实验、门禁和文档入口。
├── step.md                                         —— 持续记录各 Phase 的实现过程、验证结果、进度和运行指令。
└── tree.txt                                        —— 展示当前项目文件结构并用中文说明每个文件的作用。

说明：`.cache`、`.tools/go`、`.tools/k6`、`tests/load/.generated`、`docs/results/phase2/raw`、`web/node_modules` 和 `web/dist` 均为可再生成的缓存、工具链、匿名测试数据、原始结果或第三方构建产物；`.git` 为版本控制内部数据，因此不展开内部文件。
